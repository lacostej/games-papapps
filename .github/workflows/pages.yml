name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          package-manager-cache: false
      - run: npm test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v6
      # Ship only what the browser loads: no tests, build tools, word sources or docs.
      - run: >
          rsync -a
          --exclude='.git*' --exclude='.github/' --exclude='.claude/' --exclude='.DS_Store'
          --exclude='test/' --exclude='tools/' --exclude='sources/' --exclude='node_modules/'
          --exclude='package*.json' --exclude='*.md' --exclude='/serve.mjs'
          ./ _site/
      - uses: actions/upload-pages-artifact@v5
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v5
